# Link-Hub — Content & Order Update (data-only)

## Scope
The visual redesign (charcoal/olive "stealth-wealth" identity, Schibsted Grotesk + Inter + Geist Mono, numbered `01/02/03` chapter eyebrows with hairline dividers, typographic link rows, scroll-fill rules, static status line) is **already implemented** in `index.html`. This pass changes **only the content, order, name, and avatar initial** in the hand-editable data block at the top of the `<script>`. **Do NOT touch any CSS, layout, HTML structure, or the render script logic** — only the `PROFILE` and `SECTIONS` data values (and confirm `TICKER_ITEMS` stays as-is).

## Target file
`C:\Users\Anthony\IdeaProjects\my-links-site\index.html` — the `PROFILE` and `SECTIONS` constants near the top of the `<script>`.

## Exact changes

### PROFILE
- `name`: `"stxbandz"` (was `"Anthony"`)
- `initial`: `"S"` (was `"A"`)
- `tagline`: unchanged

### SECTIONS — new order, labels, entries (keep `icon: ""`, no emoji)

`01 / Handles`
- `{ icon: "", title: "TikTok", subtitle: "@silksnypz", url: "https://www.tiktok.com/@silksnypz" }`
- `{ icon: "", title: "Instagram", subtitle: "@stxbandz", url: "https://www.instagram.com/stxbandz/" }`
- (no placeholder row — only these two links)

`02 / Trading/Fintech`   *(label exactly "Trading/Fintech")*
- `{ icon: "", title: "Broker name", subtitle: "referral link", url: "#" }`
- `{ icon: "", title: "Trading platform", subtitle: "referral link", url: "#" }`
- `{ icon: "", title: "Fintech app", subtitle: "referral link", url: "#" }`

`03 / Products`   *(label changed from "Elsewhere" to "Products"; same position 03)*
- `{ icon: "", title: "Product name", subtitle: "referral link", url: "#" }`
- `{ icon: "", title: "Product name", subtitle: "referral link", url: "#" }`

### Unchanged
- `TICKER_ITEMS`: leave exactly as currently in file (`["Selected works", "2026", "By appointment"]`).
- All CSS, layout, chapter numbering rendering, status line, footer, and the render script — no edits.
- Section numbers `01/02/03` are auto-generated by the render script from array order, so they will match automatically.

## Constraints
- Do NOT invent specific brand names, handles, or emails — only the two real handles provided are used; everything else is the generic placeholders above.
- Do NOT modify CSS or layout code (explicit user instruction).

## Validation
- Open `index.html`: name shows `stxbandz`, avatar ring shows `S`.
- Sections render top-to-bottom as `01 Handles`, `02 Trading/Fintech`, `03 Products`.
- Handles section contains only TikTok (→ tiktok.com/@silksnypz) and Instagram (→ instagram.com/@stxbandz).
- No emoji icons; generic placeholder rows use `#` URLs.
- Visual style (colors, type, spacing, dividers, hover underline) is unchanged from current.
